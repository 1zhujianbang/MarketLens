# README.md

基于大语言模型和知识图谱构建的智能新闻处理与分析系统，通过多智能体协作实现从"新闻→实体→事件→知识图谱"的自动化处理流程。

## 项目概述

本项目旨在构建一个智能的新闻处理与分析系统，通过融合多源新闻数据，自动抽取实体、事件及其关联关系，动态构建可解释的知识图谱，并实现智能的新闻去重、实体扩展和知识推理，为用户提供结构化的新闻洞察和知识发现能力。

### 系统架构

```
新闻源 → Agent1(提取) → Agent2(扩展) → Agent3(图谱构建)
    ↓         ↓            ↓            ↓
多源API   实体/事件提取  相关新闻搜索   知识图谱压缩
```

### 核心架构重构 

系统已完成重大架构重构，实现软耦合和模块化：

- **解耦设计**: Agent只负责流程编排，业务逻辑封装在functions模块
- **统一工具层**: utils模块提供标准化的数据操作、文件处理、LLM调用等功能
- **异步增强**: 集成AsyncExecutor和RateLimiter，支持高并发处理
- **测试完备**: 为所有重构模块提供完整的单元测试覆盖

### 核心功能

|功能|状态|描述|
|---|---|---|
|多源新闻数据接入|✅|支持GNews API等多渠道新闻源|
|智能实体事件提取|✅|基于LLM的实体和事件自动抽取|
|新闻内容去重|✅|SimHash算法实现精确去重|
|实体扩展搜索|✅|根据实体关键词扩展相关新闻|
|知识图谱构建|✅|动态维护实体-事件关联网络|
|并发异步处理|✅|基于AsyncExecutor的高效并发|
|配置管理系统|✅|多文件配置支持和验证|
|统一错误处理|✅|标准异常类和错误处理机制|
|架构重构|✅|实现软耦合的模块化设计|
|测试覆盖|✅|完整的单元测试覆盖率|
|异步文件操作|✅|原子写入和文件锁机制|

## 快速开始

### 环境要求
- Python 3.8+，越新越好
- 依赖包：见 requirements.txt

### 安装步骤
1. 克隆项目
```bash
git clone https://github.com/1zhujianbang/MarketLens.git
cd MarketLens
```

2. 安装依赖
```bash
pip install -r requirements.txt
```

3. 配置环境变量
```bash
cp config/.env.example config/.env.local
# 编辑 config/.env.local 配置API密钥
```

4. 运行系统
```bash
streamlit run app.py
```
### 典型使用流程

1. **首次使用**:
   - 访问系统设置页面配置Agent参数
   - 在 Pipeline Builder（高级控制台）中配置数据源（启用/禁用）

2. **新闻处理**:
   - 在 Pipeline Builder 的 **🚀 快速运行**中选择“增量更新（默认）/初始化构建”
   - 可选：设置关键词/时间范围（抓取条件）
   - 运行后系统自动提取实体与事件，并更新知识图谱

3. **知识拓展**:
   - 在"知识拓展"标签中添加感兴趣的实体
   - 执行拓展任务发现更多相关信息

4. **结果查看**:
   - 在Dashboard查看处理统计
   - 在 Data Inspector 审查最近新增（实体/事件）与 runs 变更包
   - 在 Knowledge Graph（PyVis）宏观查看结构变化；Timeline 查看聚焦实体的时间线


## 项目结构

```
MarketLens/
├── src/                          # 源代码目录
│   ├── core/                     # 核心框架 (专注业务管道)
│   │   ├── config.py            # 配置管理
│   │   ├── data_utils.py        # 数据管道和规范化
│   │   ├── async_file_utils.py  # 异步文件操作
│   │   ├── exceptions.py        # 异常处理
│   │   ├── singleton.py         # 单例模式
│   │   └── ...
│   ├── agents/                  # 智能体模块 (仅流程编排)
│   │   ├── agent1.py           # 新闻提取智能体
│   │   ├── agent2.py           # 实体扩展智能体
│   │   └── agent3.py           # 图谱构建智能体
│   ├── functions/              # 业务逻辑封装 (解耦核心功能)
│   │   ├── data_fetch.py       # 数据获取和扩展
│   │   ├── extraction.py       # 实体和事件提取
│   │   ├── graph_ops.py        # 图谱操作和管理
│   │   └── reporting.py        # 报告生成
│   ├── web/                    # Streamlit 前端框架（多页应用）
│   │   ├── framework/          # 统一页面初始化、状态/上下文（project_id/role）
│   │   ├── pages_impl/         # 各页面的渲染实现（render()）
│   │   ├── components/         # 可复用组件（Task Monitor 等）
│   │   ├── services/           # 前端侧服务（run_store/news_lookup/pipeline_runner）
│   │   └── utils.py            # 前端数据加载与缓存（load_entities/load_events/...）
│   └── utils/                  # 通用工具模块 (标准化接口)
│       ├── json_utils.py       # JSON解析工具
│       ├── file_utils.py       # 文件和目录操作
│       ├── data_utils.py       # 数据持久化和序列化
│       ├── data_ops.py         # 高级数据操作
│       ├── llm_utils.py        # LLM调用和异步执行器
│       └── tool_function.py    # 基础工具函数
├── config/                     # 配置文件
│   ├── base.yaml              # 基础配置
│   └── agents/                # 智能体配置
├── data/                      # 数据存储
│   ├── entities.json          # 实体库
│   ├── events.json            # 事件库
│   └── knowledge_graph.json   # 知识图谱
│   └── projects/<project_id>/ # 多项目命名空间（为未来多人/公网做预置）
│       ├── runs/              # run change packs（新增实体/事件、证据链）
│       ├── evidence/          # Pin（备注/原文片段，占位低存储）
│       └── cache/pyvis/       # PyVis HTML 跨会话缓存
├── tests/                     # 测试套件
│   ├── test_data_ops.py       # 数据操作测试
│   ├── test_llm_utils.py      # LLM工具测试
│   ├── test_async_utils.py    # 异步工具测试
│   └── ...
└── requirements.txt           # 依赖配置
```

## 部署说明

### Docker部署 (推荐)

系统提供了完整的Docker容器化支持，确保环境一致性和易于部署。

#### 使用Docker Compose
```bash
# 1. 克隆项目
git clone https://github.com/1zhujianbang/MarketLens.git
cd MarketLens

# 2. 配置环境变量
cp config/.env.example config/.env.local
# 编辑config/.env.local配置API密钥

# 3. 启动服务
docker-compose up -d

# 4. 访问应用
# 打开浏览器访问 http://localhost:8501
```

#### 单独使用Docker
```bash
# 构建镜像
docker build -t MarketLens .

# 运行容器
docker run -p 8501:8501 \
  -v $(pwd)/data:/app/data \
  -v $(pwd)/config:/app/config \
  --env-file config/.env.local \
  news-agent-system
```

#### Docker Compose配置说明
- **Web服务**: Streamlit应用 (端口8501)
- **数据持久化**: 自动挂载data和config目录
- **健康检查**: 内置健康检查机制
- **环境隔离**: 完整的环境隔离和依赖管理

## 配置说明

系统采用多文件配置架构：

- `config/base.yaml`: 基础配置
- `config/agents/agent1.yaml`: Agent1配置
- `config/agents/agent2.yaml`: Agent2配置
- `config/agents/agent3.yaml`: Agent3配置


## 市场分析智能体

## API使用

### 新闻处理
```python
from src.agents.agent1 import main as process_news
import asyncio

# 运行新闻处理流程 (解耦后的Agent调用)
asyncio.run(process_news())
```

### 实体扩展
```python
from src.functions.data_fetch import expand_news_by_recent_entities
import asyncio

# 根据实体扩展新闻 (直接调用业务函数)
result = await expand_news_by_recent_entities(
    keywords=["苹果公司"],
    limit_per_entity=10
)
```

### 图谱构建
```python
from src.functions.graph_ops import refresh_graph

# 刷新知识图谱 (直接调用图谱操作)
result = refresh_graph()
```

### 数据操作工具
```python
from src.utils.data_ops import update_entities, merge_entity_data
from src.utils.llm_utils import AsyncExecutor, RateLimiter

# 更新实体库
update_entities(["苹果公司"], ["Apple Inc."], "manual")

# 异步执行器
executor = AsyncExecutor(max_concurrent=10)
results = await executor.run_concurrent_tasks([task1, task2])

# 限速器
limiter = RateLimiter(rate_per_sec=5.0)
await limiter.acquire_async()  # 获取令牌
```

## 开发指南

### 🏗️ 架构重构指南

#### 开发模式
- **Agent层**: 仅负责流程编排，调用functions模块的业务逻辑
- **Functions层**: 封装核心业务逻辑，实现功能复用
- **Utils层**: 提供标准化工具函数，避免重复代码
- **Core层**: 基础设施层

#### 添加新的业务功能
1. 在 `src/functions/` 下创建业务模块
2. 使用 `src/utils/` 中的标准化工具
3. 在Agent中调用业务函数进行编排

#### 添加新的工具函数
1. 在 `src/utils/` 下添加相应的工具模块
2. 提供统一的接口和错误处理
3. 添加完整的单元测试

### 添加新的智能体
1. 继承 `BaseAgent` 类
2. 在 `_execute_main` 中调用functions模块的业务逻辑
3. 在 `AgentManager` 中注册

### 添加新的数据源
1. 在 `src/functions/data_fetch.py` 中实现数据获取逻辑
2. 使用 `src/utils/` 中的工具进行数据处理
3. 在配置中添加数据源定义

### 扩展配置
1. 在相应配置文件中添加新配置项
2. 在 `ConfigManager` 中添加验证逻辑
3. 更新类型注解

## 🎨 UI开发规范

## 🎛️ 前端（Streamlit）框架说明

本项目使用 Streamlit **原生多页应用**能力：

- **入口**: `app.py`
- **页面路由**: `pages/*.py`（薄封装，不承载业务逻辑）
- **页面实现**: `src/web/pages_impl/*.py`（每页一个 `render()`）
- **统一初始化**: `src/web/framework/page.py` 的 `init_page()` + `PageSpec`
- **用户上下文**: `src/web/framework/user_context.py`
  - 提供 `project_id` 与 `role`（viewer/editor/admin）的轻量控制
  - 用于决定部分落盘命名空间与写权限（占位实现，便于未来公网多人）

### 🧪 Run Pack / Evidence Pin（审查优先）

每次运行流程后，前端会生成一个轻量审查包（run change pack），用于“看结果为主”的工作流：

- **Run 审查包**: `data/projects/<project_id>/runs/run_<run_id>.json`
  - `new_entities/new_events/duplicate_events`
  - `evidence_events`: 事件证据链（来自 extracted_events + raw_news best-effort）
- **Pin 备注/片段**（低存储占位）: `data/projects/<project_id>/evidence/`

### 🕸️ Knowledge Graph（PyVis）与缓存

- KG 页使用 PyVis 展示复杂图谱（按钮触发生成，避免每次进入都重算）
- 支持 PyVis HTML 跨会话缓存：`data/projects/<project_id>/cache/pyvis/`
- KG 展示支持“展示时间窗（24h/7d/全部）”，仅影响展示与审查，不影响抓取。

### Streamlit Key 管理

为了防止重复key错误，项目采用了统一的Key管理机制：

#### 🔧 使用Key管理器
```python
from src.web.streamlit_key_manager import get_unique_key, KeyContext

# 生成唯一key
key = get_unique_key('gnews', 'category', context='config_tab')

# 使用上下文管理器
with KeyContext('config_tab'):
    category_key = get_unique_key('gnews', 'category')
    query_key = get_unique_key('gnews', 'query')
```

## 许可证

[Apache License 2.0](LICENSE)

如有问题请通过GitHub Issues联系。